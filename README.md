# EAI Mobile V4

**Earth Alert Intelligence**  
**Digital Twin & Decision Intelligence Platform**

## Deploy to GitHub Pages
Upload the contents of this folder to the repository root. Keep the `dashboard/` and `assets/` folders intact.

- Mobile: `/EAIV4-Mobile-WebApp/`
- Operations Dashboard: `/EAIV4-Mobile-WebApp/dashboard/`

The visible hero no longer displays the pilot location. Existing default area values remain in `config.js` so field reporting continues to work with the current API.
