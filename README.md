# EAI Mobile V3.0

Mobile form: `/EAIV4-Mobile-WebApp/`

Dashboard: `/EAIV4-Mobile-WebApp/dashboard/`

## Deploy
Upload all files and folders in this package to the root of the GitHub repository, preserving the `dashboard/` and `assets/` folders. Commit changes, wait for GitHub Pages, then hard refresh.
